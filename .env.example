# Names only; do not put real values in a committed file.
# Configure these as encrypted gateway deployment secrets.
APP_ACCESS_TOKEN=generate-a-random-personal-gateway-token
OPENAI_API_KEY=set-with-your-gateway-secret-manager
OPENAI_MODEL=set-a-vision-capable-responses-api-model
USDA_API_KEY=set-with-your-gateway-secret-manager

# The Android app stores only the HTTPS gateway URL and APP_ACCESS_TOKEN through its encrypted Settings screen.