# Third-party notices

The Android application declares AndroidX, Jetpack Compose, Health Connect, Kotlin/kotlinx serialization/coroutines, and JUnit/AndroidX Test dependencies. They retain their respective licenses.

The gateway targets the Cloudflare Workers runtime but uses no bundled npm runtime dependency. Deployment is subject to the selected platform's terms.

USDA FoodData Central content is United States government data. Cached and exported reference values retain their FDC id, data type, URL, retrieval time, and per-field provenance. OpenAI output is an unverified estimate unless a visible package-label field is reviewed by the user; it is never represented as USDA data.